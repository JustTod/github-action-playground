name: Test switch case

on: [push]
  # pull_request:
  #   types:
  #     - closed

jobs:
  before_script:
    name: check branch
    runs-on: ubuntu-latest
    outputs:
      env_stage: ${{ steps.switch-case.outputs.match }}
    steps:
    - name: Run switch-case-action
      uses: thaitype/actions-switch-case@v1
      id: switch_case
      with:
        default: none
        conditionals-with-values: |
          ${{ github.ref_name == 'main' }} => prod
          ${{ github.ref_name == 'staging' }} => stage
    #       ${{ github.ref_name == 'develop' }} => dev
    # - name: setup output
    #   run: echo "stage=${{ steps.switch_case.outputs.match }}" >> "$GITHUB_OUTPUT"
    # - name: setup env
    #   run: echo "env_stage=${{ steps.switch_case.outputs.match }}" >> $GITHUB_ENV

  check_output:
    name: check output
    runs-on: ubuntu-latest
    needs: before_script
    env:
      STAGE: ${{ needs.before_script.outputs.env_stage }}
    # if: ${{ github.event.pull_request.merged }} && !($STAGE == "none")
    steps:
    - run: echo ${{ needs.before_script.outputs.env_stage }}
    - name: check output 
      run: echo $STAGE1
